{
    "ConnectionStrings": {
        "DefaultConnection": "server=<server>;userid=<user>;pwd=<password>;port=<port>;database=<database_name>;sslmode=none;"
    },
    "Logging": {
        "LogLevel": {
            "Default": "Debug"
        }
    }
}